# (...) Configurations and setups above


# Just call it here, I created it to not need to put the code of checking errors in every profile of every user that uses a WM without a Display Manager...
wm-initer.sh

printf "Finished!\n"
